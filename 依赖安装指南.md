# 📦 EPIC 项目依赖安装指南

## ❌ 当前问题

运行时出现错误：
```
ModuleNotFoundError: No module named 'langchain_openai'
```

**原因**：缺少 LangChain 相关的包

---

## ✅ 解决方案（3种方法）

### **方法一：一键安装脚本（最简单）** ⭐

#### 在 PowerShell 中运行：
```powershell
cd E:\中央财经大学\大学三年级\数据挖掘\EPIC
.\install_dependencies.ps1
```

#### 或者在 CMD 中运行：
```cmd
cd E:\中央财经大学\大学三年级\数据挖掘\EPIC
install_dependencies.bat
```

---

### **方法二：使用 requirements.txt（推荐）**

```bash
cd E:\中央财经大学\大学三年级\数据挖掘\EPIC\synthetic-tabular-LLM
pip install -r requirements.txt
```

**优点**：
- ✅ 安装所有项目依赖
- ✅ 版本完全匹配
- ✅ 避免兼容性问题

**缺点**：
- ⏱️ 可能需要 5-10 分钟
- 📦 会安装一些不必要的包

---

### **方法三：手动安装核心包（最快）**

只安装运行 EPIC 生成所需的核心包：

```bash
# 核心 LangChain 包
pip install langchain==0.1.13
pip install langchain-core==0.1.23
pip install langchain-openai==0.1.1
pip install langchain-community==0.0.20

# 机器学习包（评估时需要）
pip install xgboost==2.0.3
pip install catboost==1.2.3
pip install lightgbm
pip install scikit-learn==1.3.1
pip install imbalanced-learn

# 工具包
pip install tabulate
pip install tqdm
```

---

## 📋 完整依赖清单

### **已安装的包** ✅
- `httpx==0.28.0`
- `openai==2.1.0`
- `pandas==2.2.3`

### **需要安装的包** ❌

#### 核心包（必需）
| 包名 | 版本 | 用途 |
|------|------|------|
| `langchain` | 0.1.13 | LLM 框架核心 |
| `langchain-core` | 0.1.23 | LangChain 核心组件 |
| `langchain-openai` | 0.1.1 | OpenAI/DeepSeek API 接口 |
| `langchain-community` | 0.0.20 | 社区工具 |

#### 机器学习包（评估时需要）
| 包名 | 版本 | 用途 |
|------|------|------|
| `xgboost` | 2.0.3 | XGBoost 分类器 |
| `catboost` | 1.2.3 | CatBoost 分类器 |
| `lightgbm` | 最新 | LightGBM 分类器 |
| `scikit-learn` | 1.3.1 | 机器学习工具 |
| `imbalanced-learn` | 最新 | 不平衡数据处理 |

#### 工具包
| 包名 | 用途 |
|------|------|
| `tabulate` | 表格输出 |
| `tqdm` | 进度条显示 |

---

## 🚀 推荐安装流程

### **步骤 1：安装核心 LangChain 包**

```bash
pip install langchain==0.1.13 langchain-core==0.1.23 langchain-openai==0.1.1 langchain-community==0.0.20
```

**预计时间**：2-3 分钟

---

### **步骤 2：测试生成脚本**

```bash
cd E:\中央财经大学\大学三年级\数据挖掘\EPIC\synthetic-tabular-LLM\codes\SyntheticDataGeneration
python generate_samples_Travel.py
```

如果能正常运行，说明核心依赖已满足！

---

### **步骤 3：安装评估所需的包（可选）**

如果步骤 2 成功，再安装评估所需的包：

```bash
pip install xgboost==2.0.3 catboost==1.2.3 lightgbm scikit-learn==1.3.1 imbalanced-learn tabulate tqdm
```

**预计时间**：3-5 分钟

---

## ⚠️ 常见问题

### 1. 安装速度慢

**解决**：使用国内镜像源

```bash
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple langchain==0.1.13
```

---

### 2. 版本冲突

**错误示例**：
```
ERROR: pip's dependency resolver does not currently take into account all the packages that are installed.
```

**解决**：
```bash
# 先卸载旧版本
pip uninstall langchain langchain-openai langchain-core -y

# 再重新安装
pip install langchain==0.1.13 langchain-core==0.1.23 langchain-openai==0.1.1
```

---

### 3. 权限问题

**错误示例**：
```
ERROR: Could not install packages due to an OSError: [WinError 5] 拒绝访问
```

**解决**：使用 `--user` 参数
```bash
pip install --user langchain==0.1.13
```

---

### 4. OpenAI 版本不兼容

你当前的 `openai==2.1.0` 可能与 `langchain-openai==0.1.1` 不兼容。

**如果出现问题，降级 OpenAI**：
```bash
pip install openai==1.14.3
```

---

## 🔍 验证安装

安装完成后，运行以下命令验证：

```bash
python -c "from langchain_openai import ChatOpenAI; print('✅ LangChain 安装成功')"
```

**预期输出**：
```
✅ LangChain 安装成功
```

---

## 📊 安装时间估算

| 方法 | 预计时间 | 包数量 |
|------|----------|--------|
| **方法一（脚本）** | 5-8 分钟 | ~15 个 |
| **方法二（requirements.txt）** | 10-15 分钟 | ~80 个 |
| **方法三（手动核心包）** | 2-3 分钟 | ~4 个 |

---

## 🎯 推荐方案

### 如果你只想运行生成脚本：
👉 **使用方法三**（只安装核心包，2-3分钟）

### 如果你想运行完整流程（生成+评估）：
👉 **使用方法一**（一键脚本，5-8分钟）

### 如果你想确保完全兼容：
👉 **使用方法二**（requirements.txt，10-15分钟）

---

## 🚀 快速开始

**最快的方式**（只需 2 分钟）：

```bash
# 1. 安装核心包
pip install langchain==0.1.13 langchain-core==0.1.23 langchain-openai==0.1.1

# 2. 运行生成脚本
cd E:\中央财经大学\大学三年级\数据挖掘\EPIC\synthetic-tabular-LLM\codes\SyntheticDataGeneration
python generate_samples_Travel.py
```

---

## 📞 需要帮助？

如果安装过程中遇到问题：

1. 检查 Python 版本：`python --version`（需要 3.8+）
2. 检查 pip 版本：`pip --version`（建议升级：`python -m pip install --upgrade pip`）
3. 查看错误日志，搜索关键错误信息

---

## ✅ 安装完成后

运行以下命令开始生成数据：

```bash
cd E:\中央财经大学\大学三年级\数据挖掘\EPIC\synthetic-tabular-LLM\codes\SyntheticDataGeneration
python generate_samples_Travel.py
```

预计 15-30 分钟后，你会得到 1000 条合成数据！🎉

